# Direct production dependencies are pinned so rebuilding the same submodule
# revision does not silently select a different application dependency set.
# mem0 is installed with --no-deps, so anything the runtime needs (including
# optional RDS IAM helpers) must be listed here explicitly.
alembic==1.14.1
anthropic==0.49.0
bcrypt==4.0.1
# Required by mem0.utils.aws_rds_iam when MEM0_RDS_IAM_AUTH=true (alembic + API).
boto3==1.38.46
cryptography==48.0.1
email-validator==2.2.0
fastapi==0.139.2
# Pin starlette to Trivy fixed floor (CVE-2025-62727, CVE-2026-48818, CVE-2026-54283)
starlette==1.3.1
fastembed==0.8.0
google-generativeai==0.8.5
groq==1.5.0
httpx==0.28.1
openai==1.90.0
passlib==1.7.4
posthog==7.14.0
protobuf==5.29.6
# [binary] ships psycopg_binary + bundled libpq; required on python:slim (no system libpq).
psycopg[binary]==3.2.8
psycopg-pool==3.2.6
pydantic==2.10.4
python-dotenv==1.2.2
# Trivy CRITICAL CVE-2024-33663 (algorithm confusion)
python-jose==3.5.0
pytz==2025.1
qdrant-client==1.13.2
slowapi==0.1.9
sqlalchemy==2.0.38
uvicorn==0.34.0
# Change trail: @hungxqt - 2026-07-20 - Bump cryptography/python-jose/fastapi/starlette for Trivy
