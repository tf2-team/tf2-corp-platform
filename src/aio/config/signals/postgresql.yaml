schema_version: "1.0"
enabled: false
signals:
  - signal_id: postgresql_backend_usage_ratio_5m
    owner: aio4
    source_adapter: prometheus
    query_ref: queries/postgresql.yaml#postgresql_backend_usage_ratio_5m
    unit: ratio
    shape: scalar
    cadence: 60s
    window: 5m
    freshness_limit: 180s
    qualification_state: unqualified
    evidence_ref: evidence/adr-threshold-db-001-required
    purpose: diagnostic
    required_labels: []
    max_cardinality: 1
    minimum_samples: 5
  - signal_id: postgresql_active_backends_5m
    owner: aio4
    source_adapter: prometheus
    query_ref: queries/postgresql.yaml#postgresql_active_backends_5m
    unit: count
    shape: vector
    cadence: 60s
    window: 5m
    freshness_limit: 180s
    qualification_state: unqualified
    evidence_ref: evidence/adr-threshold-db-001-required
    purpose: diagnostic
    required_labels:
      - postgresql_database_name
    max_cardinality: 50
    minimum_samples: 5

