schema_version: "1.0"
enabled: false
admission_state: conditional-p1
queries:
  - query_id: aie_correctness_status
    source_adapter: aie-status
    mode: event
    window: 15m
    unit: boolean
    result_shape: event
    status: unqualified
    expression: disabled_until_aie_status_contract_exists

