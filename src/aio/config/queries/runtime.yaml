schema_version: "1.0"
enabled: false
queries:
  - query_id: aiops_scheduler_last_success_timestamp_seconds
    source_adapter: prometheus
    mode: instant
    window: 5m
    unit: seconds
    result_shape: vector
    status: unqualified
    expression: |
      aiops_scheduler_last_success_timestamp_seconds
